# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'

require 'bundler'
Bundler.setup

require 'bubble-wrap/reactor'

Motion::Project::App.setup do |app|
  app.name = 'motion-static'
  app.delegate_class = 'MotionAppDelegate'
end
